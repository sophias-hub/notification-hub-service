<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.sophias.hub</groupId>
  <artifactId>notification-hub-sdk</artifactId>
  <version>0.1.0</version>
  <name>Notification Hub SDK</name>
  <description>Java client for the Notification Hub API</description>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <jackson.version>2.18.2</jackson.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>com.fasterxml.jackson.core</groupId>
      <artifactId>jackson-databind</artifactId>
      <version>${jackson.version}</version>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-javadoc-plugin</artifactId>
        <version>3.11.2</version>
        <configuration>
          <!-- javadoc:javadoc uses outputDirectory (not reportOutputDirectory). -->
          <outputDirectory>${project.basedir}/../../docs/sdk/java</outputDirectory>
          <doctitle>Notification Hub SDK (Java)</doctitle>
          <windowtitle>Notification Hub SDK (Java)</windowtitle>
          <failOnError>true</failOnError>
          <doclint>none</doclint>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <version>3.5.0</version>
        <configuration>
          <classpathScope>test</classpathScope>
          <mainClass>com.sophias.hub.notification.SmokeTest</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
